<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signaler un bug - Nexus Net</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        :root { --accent-blue: <%= (theme && theme.accent) ? theme.accent : '#38bdf8' %>; }
    </style>
</head>
<body>
    <div class="container">
        <nav class="navbar">
            <a href="/dashboard" class="logo">NEXUS NET</a>
            <div class="nav-links">
                <a href="/dashboard" class="nav-link">Retour au Dashboard</a>
            </div>
        </nav>

        <div class="auth-container">
            <h2 class="auth-title">Signaler un bug</h2>
            
            <% if (success) { %>
                <div style="color: #4ade80; background: rgba(74, 222, 128, 0.1); padding: 10px; border-radius: 6px; margin-bottom: 20px;">
                    <%= success %>
                </div>
            <% } %>

            <form action="/report-bug" method="POST">
                <div class="form-group">
                    <label for="title">Titre du bug</label>
                    <input type="text" id="title" name="title" required placeholder="Ex: Erreur lors de la sauvegarde...">
                </div>
                <div class="form-group">
                    <label for="description">Description détaillée</label>
                    <textarea id="description" name="description" required rows="5" style="width: 100%; padding: 12px; background: rgba(0,0,0,0.3); border: 1px solid var(--border-color); border-radius: 6px; color: var(--text-primary); font-family: 'Inter', sans-serif; resize: vertical;"></textarea>
                </div>
                <button type="submit" class="btn btn-primary" style="width: 100%;">Envoyer le rapport</button>
            </form>
        </div>
    </div>
</body>
</html>
